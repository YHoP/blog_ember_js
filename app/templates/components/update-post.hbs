{{#if updatePostForm}}
  <h1>Update Post</h1>
  <div>
    <form>
      <div class="form-group">
        <label class="col-md-2" for="username">User Name</label>
        {{input value=username id="username" placeholder=post.username}}
      </div>

      <div class="form-group">
        <label class="col-md-2" for="title">Title</label>
        {{input value=title id="title" placeholder=post.title}}
      </div>

      <div class="form-group">
        <label class="col-md-2" for="text">Text</label>
        {{textarea value=text id="text" placeholder=post.text}}
      </div>

      <button class="btn btn-info" {{action 'updatePost' post}}>Update</button>
    </form>
  </div>

{{else}}
    <button class="btn btn-info" {{action 'updateFormShow'}}>Update Post</button>
{{/if}}
